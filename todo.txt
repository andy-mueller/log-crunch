-provide request parameter extraction
    Adding a new request parameter is tedious, because it has to be extracted,
    converted and copied into the query. In the near future, there will be
    more parameters to extend (about 3-4), each taking abut 10 minutes.
    A generic mechanism should be doable in 30 minutes and be better maintainable.

-introduce package for logback/slf4j implementation/format
    -Include TestLogFile!

-Provide http query mechanism
GET http://localhost:8080/logcrunch/filter?fileName=machine01-%d{yyyMMdd}.log&searchRange=2007-05-07T13:55:22,100/2009-07-02&level=Info&level=Warn

-provide decorator base class for LogFile. Fix FilterLogFile then

-parse multiline log lines